services:
  app:
    build: .
    container_name: "amur"
    env_file:
      - .env
    ports:
      - 2020:2020
    networks:
      - amur_ntwrk  
    environment:
      - TZ=Asia/Tashkent
   
networks:
  amur_ntwrk: 
    external: true